# AmbedkarGPT-Intern-Task
A Retrieval-Augmented Generation (RAG) system that answers questions from Ambedkar’s speech using LangChain, ChromaDB, HuggingFace embeddings, and the local Ollama LLaMA 3.2 model. It loads text, creates vector embeddings, retrieves relevant chunks, and generates accurate responses offline.
